# Copy this file to  .env  and fill it in.

PORT=3000
SESSION_SECRET=change-this-to-any-long-random-string

# Postgres connection string. In the Vercel dashboard: Storage -> Create
# Database -> Postgres, then "Connect" to your project — Vercel fills this
# in automatically for deployed environments. For local development, copy
# the same connection string here (or point at your own local Postgres).
POSTGRES_URL=

# Vercel Blob storage, used for product/barcode photo uploads. In the
# Vercel dashboard: Storage -> Create Database -> Blob, then "Connect" to
# your project. Copy the token shown there for local development too.
BLOB_READ_WRITE_TOKEN=

# The first staff login. Created when you run:  npm run seed
ADMIN_NAME=Rick Ice Admin
ADMIN_EMAIL=admin@rickice.com
ADMIN_PASSWORD=change-me

# Where order alerts are sent
SHOP_EMAIL=orders@rickice.com
SHOP_WHATSAPP=18690000000

# Email sending. Leave SMTP_HOST blank and emails are written to the
# console and the mail_log table instead of being sent — handy while testing.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM="Rick Ice Solutions <no-reply@rickice.com>"
